ip: 0.0.0.0
port: 50052
gripper:
  _target_: polymetis.robot_drivers.executable_robot_server.ExecutableRobotServer
  exec: franka_gripper_server
  cfg:
    ip: ${ip}
    port: ${port}
    robot_ip: 172.16.0.2
